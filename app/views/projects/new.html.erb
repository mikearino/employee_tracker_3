<h1>Add a new project:</h1>

<%= form_for [@employee, @project] do |f| %>

  
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <p><%= f.submit %></p>
<% end %>
